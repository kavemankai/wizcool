# Phase 3 — Tag-based upgrade interaction engine (commercial foundation)
# Core tags: Fire, Ice, Lightning, Poison, Bounce, Split, Critical, Arcane, Shield
# Each projectile carries Array[String] tags; interactions resolve here at hit time
# DEBUG: print tag state on every resolution until game ships
extends Node
